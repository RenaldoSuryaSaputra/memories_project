const PostsStyle = (() => ({
   mainContainer: {
      display: "flex",
      alignItems: "center",
   },
   smMargin: {
      // margin: theme.spacing(1),
      margin: "4px",
   },
   actionDiv: {
      textAlign: "center",
   },
}));

export default PostsStyle;
